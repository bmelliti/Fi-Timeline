{
  "name": "fi-timeline-calculator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "cd frontend && npm install",
    "build": "cd frontend && npm install && npm run build",
    "start": "npx serve@latest frontend/build"
  },
  "dependencies": {
    "serve": "^14.0.0"
  },
  "engines": {
    "npm": ">=8.0.0",
    "yarn": "please-use-npm"
  }
}
